# Local-only Mesh Signal artifact selection.
# Relative paths resolve from the Prime Observer repository root.
MESH_SIGNAL_ARTIFACT_PATH=../mesh-signal/mesh_signal.json
# Optional override. When empty, Prime reads Mesh Signal's standard owner-only history store.
MESH_SIGNAL_HISTORY_PATH=
